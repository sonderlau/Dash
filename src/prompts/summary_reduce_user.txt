请基于以下多段论文阶段性摘要，整理出整篇论文的最终中文总结。

目标语言：{language}

标题：{title}
匹配分类：{categories}

阶段性摘要集合：
{chunk_summaries}

请严格输出 JSON 对象，不要输出 markdown，不要输出代码块。
JSON 字段固定为：
- "tldr"
- "motivation"
- "method"
- "result"
- "conclusion"

要求：
- 对重复信息做合并，不要机械拼接。
- `tldr` 要给出整篇论文层面的完整总结。
- `motivation`、`method`、`result`、`conclusion` 需要综合所有片段，而不是只抄单个片段。
- 如果不同片段信息不一致，以更完整、更具体的内容为准；若无法确认，不要编造。

每个字段都使用目标语言填写，允许空字符串，但不要编造信息。
