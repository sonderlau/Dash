请基于以下论文信息生成中文总结。

目标语言：{language}

标题：{title}
匹配分类：{categories}
英文摘要：
{abstract_en}

提取出的论文全文（可能保留了 markdown 结构）：
{fulltext_context}

请严格输出 JSON 对象，不要输出 markdown，不要输出代码块。
JSON 字段固定为：
- "tldr"
- "motivation"
- "method"
- "result"
- "conclusion"

请按“论文阅读笔记”的方式来写：
- `tldr` 是几句话组成的完整总结，不要只写一句口号式短句。
- `motivation`、`method`、`result`、`conclusion` 是分项研究笔记，不要写成过短的标签词。
- 可以简洁，但不需要为了省 token 把句子压缩得不自然。
- 如果正文信息比摘要更完整，以正文为准；但不要编造正文中没有明确支持的实验细节。

每个字段都使用目标语言填写，允许空字符串，但不要编造信息。
