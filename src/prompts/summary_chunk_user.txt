请基于以下论文片段生成中文阶段性阅读笔记。

目标语言：{language}

标题：{title}
匹配分类：{categories}
这是第 {chunk_index} / {chunk_total} 个片段。

论文片段内容：
{chunk_content}

请严格输出 JSON 对象，不要输出 markdown，不要输出代码块。
JSON 字段固定为：
- "tldr"
- "motivation"
- "method"
- "result"
- "conclusion"

要求：
- 这是阶段性摘要，不必覆盖整篇论文，只总结当前片段里明确出现的信息。
- `tldr` 写成 2-4 句自然中文。
- `motivation`、`method`、`result`、`conclusion` 可以留空，但不要编造。
- 保留关键技术术语，必要时附英文原词。

每个字段都使用目标语言填写，允许空字符串，但不要编造信息。
